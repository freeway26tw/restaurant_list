<div class="container mt-5">
  <div class="row">
    <div class="col-md-10 col-12 mx-auto">
      <div class="row row-cols-sm-3">

        <form action="/restaurants" method="POST">
          {{{formInput 'id' 'ID' 'text'}}}
          {{{formInput 'name' 'Name' 'text'}}}
          {{{formInput 'name_en' 'Name(en)''text'}}}
          {{{formInput 'category' 'Category''text'}}}
          {{{formInput 'image' 'Image''text'}}}
          {{{formInput 'location' 'Location''text'}}}
          {{{formInput 'phone' 'Phone''text'}}}
          {{{formInput 'google_map' 'Google Map''text'}}}
          {{{formInput 'rating' 'Rating''number'}}}
          {{{formInput 'description' 'Description''text'}}}
          <button type="submit">Create</button>
        </form>

      </div>
    </div>
  </div>
</div>